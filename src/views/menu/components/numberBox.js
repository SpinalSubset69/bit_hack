import React, { useState } from 'react';

export default function NumberBoxComponent({ number }){
    const [localNumber, setLocalNumber] = useState(number);
    return(
        <div>
            
        </div>
    );
}